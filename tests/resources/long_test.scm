;; 第1行：这是一个很长的测试文件
;; 第2行：用于测试上下文分隔线功能
;; 第3行：文件包含多行内容
;; 第4行：这样可以看到分隔线效果
;; 第5行：继续添加内容
;; 第6行：更多内容
;; 第7行：测试代码开始
;; 第8行：定义一个函数
;; 第9行：这里有一个括号问题
(define (problem-function x y z
  ;; 第11行：函数体开始
  (let ((a (+ x y))
        (b (* y z))
        (c (- z x)))
    ;; 第15行：进行计算
    (+ a b c)
  ;; 第17行：这里缺少闭括号
  ;; 第18行：更多注释
  ;; 第19行：文件继续
  ;; 第20行：最后一行